# Core settings
APP_ENV=local
API_V1_STR=/api/v1
SECRET_KEY=changeme123secretkeylocaldev
ACCESS_TOKEN_EXPIRE_MINUTES=10080  # 7 days

# CORS settings (comma-separated list)
BACKEND_CORS_ORIGINS=http://localhost,http://localhost:3000,http://localhost:8000

# Database settings
DATABASE_TYPE=postgres

# PostgreSQL settings
POSTGRES_SERVER=localhost
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=app
POSTGRES_PORT=5432

# Oracle settings (if needed)
ORACLE_SERVER=localhost
ORACLE_USER=system
ORACLE_PASSWORD=oracle
ORACLE_SERVICE=xe
ORACLE_PORT=1521

# Redis settings
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=

# Cache settings
CACHE_EXPIRATION_SECONDS=300  # 5 minutes

# MinIO settings
MINIO_HOST=localhost
MINIO_PORT=9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=media
MINIO_SECURE=false

# API settings
DISABLE_DOCS=false

# Rate limiting
RATE_LIMIT_ENABLED=true
RATE_LIMIT_PER_SECOND=10

# Logging
LOG_LEVEL=DEBUG